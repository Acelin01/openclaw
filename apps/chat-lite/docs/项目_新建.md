以下是优化后的项目新建弹框HTML。左侧为模板选择区，右侧为动态项目信息表单，右上角增加关闭按钮。点击不同模板，右侧项目名称、编号、描述会自动填充对应默认值（编号前缀+随机数），方便演示联动效果。

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>选择模板 · 创建项目</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Roboto, system-ui, sans-serif;
        }

        body {
            background: linear-gradient(145deg, #e0e4e8 0%, #f2f5f9 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        /* 弹窗卡片 — 左右布局，继承之前风格 */
        .project-modal {
            display: flex;
            width: 100%;
            max-width: 1300px;
            background: #fff;
            border-radius: 28px;
            overflow: hidden;
            box-shadow: 0 30px 60px rgba(0, 0, 0, 0.2);
            position: relative;
        }

        /* 左侧模板选择区 (原绿色区域，现作为模板导航) */
        .modal-left {
            flex: 1 1 36%;
            background: #1dbf73;      /* Fiverr 绿 */
            color: white;
            padding: 32px 24px;
            display: flex;
            flex-direction: column;
            position: relative;
            overflow-y: auto;
            max-height: 800px;
        }

        .left-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 28px;
        }

        .left-header h2 {
            font-size: 24px;
            font-weight: 600;
            letter-spacing: 0.5px;
        }

        .template-manage {
            background: rgba(255,255,255,0.2);
            padding: 8px 16px;
            border-radius: 40px;
            font-size: 14px;
            font-weight: 500;
            cursor: pointer;
            transition: background 0.2s;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .template-manage:hover {
            background: rgba(255,255,255,0.35);
        }

        .template-list {
            display: flex;
            flex-direction: column;
            gap: 16px;
            margin-bottom: 30px;
        }

        .template-item {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(4px);
            border-radius: 20px;
            padding: 20px 18px;
            border: 2px solid transparent;
            transition: all 0.2s;
            cursor: pointer;
            display: flex;
            align-items: flex-start;
            gap: 16px;
        }

        .template-item.active {
            background: rgba(255, 255, 255, 0.25);
            border-color: white;
            box-shadow: 0 10px 20px rgba(0,0,0,0.1);
        }

        .template-icon {
            background: rgba(255,255,255,0.2);
            width: 48px;
            height: 48px;
            border-radius: 18px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
            color: white;
            flex-shrink: 0;
        }

        .template-info h3 {
            font-size: 18px;
            font-weight: 700;
            margin-bottom: 6px;
        }

        .template-info p {
            font-size: 14px;
            opacity: 0.9;
            line-height: 1.5;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .template-badge {
            margin-top: 8px;
            background: rgba(255,255,255,0.15);
            display: inline-block;
            padding: 4px 12px;
            border-radius: 30px;
            font-size: 12px;
        }

        .pattern-bg {
            position: absolute;
            bottom: -20px;
            right: -20px;
            font-size: 160px;
            opacity: 0.1;
            color: white;
            transform: rotate(15deg);
            pointer-events: none;
        }

        /* 右侧表单区 */
        .modal-right {
            flex: 1 1 64%;
            background: white;
            padding: 32px 40px;
            overflow-y: auto;
            max-height: 800px;
            position: relative;
        }

        /* 关闭按钮 (右上角) */
        .close-btn {
            position: absolute;
            top: 28px;
            right: 32px;
            background: #f0f2f4;
            width: 44px;
            height: 44px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 22px;
            color: #5e6166;
            cursor: pointer;
            transition: all 0.2s;
            border: none;
            z-index: 10;
        }

        .close-btn:hover {
            background: #e0e4e9;
            color: #1dbf73;
            transform: rotate(90deg);
        }

        .modal-right h1 {
            font-size: 28px;
            font-weight: 600;
            color: #222325;
            margin-bottom: 28px;
            display: flex;
            align-items: center;
            padding-right: 50px; /* 给关闭按钮留空间 */
        }

        .modal-right h1 i {
            color: #1dbf73;
            margin-right: 12px;
            font-size: 28px;
        }

        /* 表单项通用 */
        .field-group {
            margin-bottom: 28px;
        }

        .field-label {
            display: flex;
            align-items: center;
            font-weight: 600;
            color: #222325;
            font-size: 16px;
            margin-bottom: 8px;
        }

        .required-star {
            color: #e03a3a;
            margin-left: 4px;
            font-size: 18px;
        }

        .input-field, .textarea-field {
            width: 100%;
            padding: 14px 18px;
            border: 1px solid #dadbdd;
            border-radius: 10px;
            font-size: 15px;
            transition: 0.2s;
            background: #fff;
        }

        .input-field:focus, .textarea-field:focus {
            border-color: #1dbf73;
            outline: none;
            box-shadow: 0 0 0 3px rgba(29,191,115,0.1);
        }

        .textarea-field {
            min-height: 90px;
            resize: vertical;
        }

        /* 公开性双卡片 */
        .visibility-row {
            display: flex;
            gap: 20px;
            margin-top: 5px;
        }

        .visibility-card {
            flex: 1;
            border: 1px solid #dadbdd;
            border-radius: 16px;
            padding: 18px 16px;
            cursor: pointer;
            transition: all 0.2s;
            background: white;
        }

        .visibility-card.selected {
            border: 2px solid #1dbf73;
            background: #f0fcf6;
        }

        .visibility-card .title {
            font-weight: 700;
            font-size: 18px;
            margin-bottom: 6px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .visibility-card .desc {
            font-size: 14px;
            color: #62646a;
        }

        /* 进度条 */
        .progress-block {
            display: flex;
            align-items: center;
            gap: 15px;
            flex-wrap: wrap;
        }

        .progress-text {
            font-weight: 600;
            color: #1dbf73;
            background: #e5f7ed;
            padding: 6px 14px;
            border-radius: 40px;
            font-size: 15px;
        }

        .progress-bar-bg {
            flex: 1;
            height: 10px;
            background: #eceef0;
            border-radius: 20px;
            overflow: hidden;
        }

        .progress-fill {
            width: 4%; /* 初始 4/100 */
            height: 100%;
            background: #1dbf73;
            border-radius: 20px;
        }

        /* 需求管理卡片 */
        .section-title {
            font-size: 18px;
            font-weight: 700;
            color: #222;
            margin: 30px 0 18px 0;
            display: flex;
            align-items: center;
        }

        .section-title i {
            color: #1dbf73;
            margin-right: 10px;
        }

        .feature-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 15px;
            margin-bottom: 35px;
        }

        .feature-item {
            background: #f8f9fa;
            border-radius: 20px;
            padding: 22px 10px;
            text-align: center;
            border: 1px solid #edeff2;
            transition: 0.2s;
        }

        .feature-item:hover {
            border-color: #1dbf73;
            transform: translateY(-2px);
            box-shadow: 0 8px 16px rgba(0,0,0,0.03);
        }

        .feature-item i {
            font-size: 28px;
            color: #1dbf73;
            margin-bottom: 12px;
        }

        .feature-item span {
            display: block;
            font-weight: 600;
            color: #222325;
            font-size: 15px;
        }

        /* 操作按钮组 */
        .action-buttons {
            display: flex;
            gap: 18px;
            justify-content: flex-end;
            margin-top: 20px;
            border-top: 1px solid #edf0f2;
            padding-top: 30px;
        }

        .btn-outline {
            background: white;
            border: 1px solid #c2c6cc;
            color: #222325;
            padding: 14px 30px;
            border-radius: 12px;
            font-weight: 600;
            font-size: 16px;
            cursor: pointer;
            transition: 0.2s;
        }

        .btn-outline:hover {
            border-color: #1dbf73;
            color: #1dbf73;
        }

        .btn-primary {
            background: #1dbf73;
            border: none;
            color: white;
            padding: 14px 38px;
            border-radius: 12px;
            font-weight: 600;
            font-size: 16px;
            cursor: pointer;
            transition: 0.2s;
            box-shadow: 0 6px 12px rgba(29,191,115,0.2);
        }

        .btn-primary:hover {
            background: #19a463;
            transform: scale(1.02);
        }

        hr {
            border: none;
            border-top: 1px solid #eceef0;
            margin: 20px 0 10px 0;
        }

        /* 小提示 */
        .template-hint {
            margin-top: auto;
            font-size: 13px;
            opacity: 0.7;
            display: flex;
            align-items: center;
            gap: 10px;
            padding-top: 20px;
        }

        /* 响应式 */
        @media (max-width: 1000px) {
            .project-modal {
                flex-direction: column;
                max-width: 700px;
            }
            .feature-grid {
                grid-template-columns: repeat(2, 1fr);
            }
            .close-btn {
                top: 20px;
                right: 20px;
            }
        }
    </style>
</head>
<body>
    <div class="project-modal">
        <!-- 左侧模板选择区 -->
        <div class="modal-left">
            <div class="left-header">
                <h2><i class="fas fa-layer-group" style="margin-right: 8px;"></i>选择项目模板</h2>
                <div class="template-manage" id="templateManageBtn">
                    <i class="fas fa-cog"></i> 模板管理
                </div>
            </div>

            <div class="template-list" id="templateList">
                <!-- 模板将通过JS动态生成，也可以静态写，但为了便于维护，我们静态写，然后用JS绑定事件 -->
            </div>

            <div class="template-hint">
                <i class="fas fa-info-circle"></i> 选择模板后将自动预填项目信息，你可以继续修改
            </div>

            <div class="pattern-bg">
                <i class="fas fa-rocket"></i>
            </div>
        </div>

        <!-- 右侧动态表单区 + 关闭按钮 -->
        <div class="modal-right">
            <div class="close-btn" id="closeModalBtn" title="关闭">
                <i class="fas fa-times"></i>
            </div>

            <h1>
                <i class="fas fa-pen-square"></i> 填写项目信息
            </h1>

            <!-- 项目名称 -->
            <div class="field-group">
                <div class="field-label">
                    项目名称 <span class="required-star">*</span>
                </div>
                <input class="input-field" type="text" id="projectName" value="经典项目" placeholder="例如：产品设计迭代">
            </div>

            <!-- 公开性 -->
            <div class="field-group">
                <div class="field-label">
                    公开性 <span class="required-star">*</span>
                </div>
                <div class="visibility-row" id="visibilityGroup">
                    <div class="visibility-card selected" data-value="public">
                        <div class="title"><i class="fas fa-globe"></i> 公开</div>
                        <div class="desc">组织全员可访问</div>
                    </div>
                    <div class="visibility-card" data-value="private">
                        <div class="title"><i class="fas fa-lock"></i> 私密</div>
                        <div class="desc">仅成员可访问</div>
                    </div>
                </div>
            </div>

            <!-- 项目编号 -->
            <div class="field-group">
                <div class="field-label">
                    项目编号 <span class="required-star">*</span>
                </div>
                <input class="input-field" type="text" id="projectCode" value="PM-2025-001" placeholder="例如：PROJ-001">
            </div>

            <!-- 项目描述 -->
            <div class="field-group">
                <div class="field-label">项目描述</div>
                <textarea class="textarea-field" id="projectDesc" placeholder="项目描述">适用于计划驱动的项目管理，支持WBS分解、里程碑跟踪</textarea>
            </div>

            <!-- 工时统计 4/100 -->
            <div class="field-group">
                <div class="field-label">
                    工时统计 <i class="far fa-clock" style="margin-left: 8px; color:#1dbf73;"></i>
                </div>
                <div class="progress-block">
                    <span class="progress-text" id="progressText">4 / 100</span>
                    <div class="progress-bar-bg">
                        <div class="progress-fill" id="progressFill" style="width: 4%;"></div>
                    </div>
                </div>
            </div>

            <hr>

            <!-- 需求管理模块 (固定，不变) -->
            <div class="section-title">
                <i class="fas fa-tasks"></i> 需求管理
            </div>
            <div class="feature-grid">
                <div class="feature-item">
                    <i class="fas fa-clipboard-list"></i>
                    <span>需求管理</span>
                </div>
                <div class="feature-item">
                    <i class="fas fa-check-double"></i>
                    <span>任务管理</span>
                </div>
                <div class="feature-item">
                    <i class="fas fa-chart-pie"></i>
                    <span>进度管理</span>
                </div>
                <div class="feature-item">
                    <i class="fas fa-ruler-combined"></i>
                    <span>度量服务</span>
                </div>
            </div>

            <!-- 底部按钮 -->
            <div class="action-buttons">
                <button class="btn-outline" id="reselectTemplate"><i class="fas fa-th-large"></i> 重选模板</button>
                <button class="btn-primary" id="createProject"><i class="fas fa-plus-circle"></i> 新建</button>
            </div>

            <p style="font-size: 12px; color:#9b9ea4; margin-top: 20px;">
                <i class="fas fa-info-circle"></i> 带 <span style="color:#e03a3a;">*</span> 为必填项
            </p>
        </div>
    </div>

    <script>
        (function() {
            // ---------- 模板数据 ----------
            const templates = [
                {
                    id: 'classic',
                    name: '经典项目管理空间',
                    desc: '支持计划型项目管理场景，通过WBS任务拆解、里程碑规划、及任务甘特图、风险管理等功能，有效控制项目进度。',
                    icon: 'fas fa-diagram-project',
                    defaultName: '经典项目',
                    defaultDesc: '计划驱动型项目管理，包含WBS分解、里程碑、甘特图与风险跟踪。',
                    codePrefix: 'PM-'
                },
                {
                    id: 'defect',
                    name: '缺陷管理空间',
                    desc: '提供规范的缺陷记录、跟踪和灵活的流程定制，有效管理缺陷修复过程。结合全面的质量度量。',
                    icon: 'fas fa-bug',
                    defaultName: '缺陷跟踪',
                    defaultDesc: '缺陷全生命周期管理，支持自定义流程与质量分析。',
                    codePrefix: 'BUG-'
                },
                {
                    id: 'feedback',
                    name: '业务反馈管理空间',
                    desc: '支持业务团队统一管理业务需求，可更加便捷的业务需求收录、流转、进度跟踪，并提供交互分析。',
                    icon: 'fas fa-comment-dots',
                    defaultName: '业务反馈',
                    defaultDesc: '收集业务需求，跟踪流转与反馈闭环。',
                    codePrefix: 'FB-'
                },
                {
                    id: 'product',
                    name: '产品规划空间',
                    desc: '支持产品经理进行统一的产品规划能力，通过主题聚合产品需求并围绕价值进行优先级排期。',
                    icon: 'fas fa-lightbulb',
                    defaultName: '产品规划',
                    defaultDesc: '产品路线图、需求优先级、版本规划。',
                    codePrefix: 'PD-'
                },
                {
                    id: 'agile',
                    name: '敏捷研发管理空间',
                    desc: '快速、有效的落地Scrum，合理规划并形成交付节奏，持续提升敏捷交付能力。',
                    icon: 'fas fa-rotate',
                    defaultName: '敏捷研发',
                    defaultDesc: 'Scrum 敏捷开发，迭代管理、看板、燃尽图。',
                    codePrefix: 'AG-'
                }
            ];

            // ---------- 渲染左侧模板列表 ----------
            const templateListEl = document.getElementById('templateList');
            function renderTemplates() {
                let htmlStr = '';
                templates.forEach((tpl, index) => {
                    const activeClass = index === 0 ? 'active' : ''; // 默认第一个激活
                    htmlStr += `
                        <div class="template-item ${activeClass}" data-id="${tpl.id}" data-index="${index}">
                            <div class="template-icon"><i class="${tpl.icon}"></i></div>
                            <div class="template-info">
                                <h3>${tpl.name}</h3>
                                <p>${tpl.desc}</p>
                                <span class="template-badge">${tpl.codePrefix}模板</span>
                            </div>
                        </div>
                    `;
                });
                templateListEl.innerHTML = htmlStr;
            }
            renderTemplates();

            // ---------- DOM 元素 ----------
            const templateItems = document.querySelectorAll('.template-item');
            const projectNameInput = document.getElementById('projectName');
            const projectCodeInput = document.getElementById('projectCode');
            const projectDescTextarea = document.getElementById('projectDesc');
            const progressText = document.getElementById('progressText');
            const progressFill = document.getElementById('progressFill');

            // 公开性卡片切换
            const visibilityCards = document.querySelectorAll('.visibility-card');
            visibilityCards.forEach(card => {
                card.addEventListener('click', function() {
                    visibilityCards.forEach(c => c.classList.remove('selected'));
                    this.classList.add('selected');
                });
            });

            // ---------- 辅助函数：更新右侧内容根据选中模板 ----------
            function updateFormByTemplate(template) {
                if (!template) return;
                projectNameInput.value = template.defaultName;
                // 生成编号：前缀 + 日期后缀 + 随机数（简单演示）
                const now = new Date();
                const year = now.getFullYear().toString().slice(-2);
                const month = (now.getMonth() + 1).toString().padStart(2, '0');
                const random = Math.floor(Math.random() * 100).toString().padStart(3, '0');
                projectCodeInput.value = `${template.codePrefix}${year}${month}-${random}`;
                projectDescTextarea.value = template.defaultDesc;
                // 工时进度保持不变（演示用，也可调整）
                // progressText.innerText = '4 / 100';
                // progressFill.style.width = '4%';
            }

            // 获取模板对象通过id
            function getTemplateById(id) {
                return templates.find(t => t.id === id) || templates[0];
            }

            // 切换模板选中状态
            function setActiveTemplate(targetItem) {
                templateItems.forEach(item => item.classList.remove('active'));
                targetItem.classList.add('active');
                const id = targetItem.dataset.id;
                const template = getTemplateById(id);
                updateFormByTemplate(template);
            }

            // 为每个模板项绑定点击事件
            templateItems.forEach(item => {
                item.addEventListener('click', function(e) {
                    setActiveTemplate(this);
                });
            });

            // 默认加载第一个模板
            if (templateItems.length > 0) {
                const firstId = templateItems[0].dataset.id;
                const firstTemplate = getTemplateById(firstId);
                updateFormByTemplate(firstTemplate);
            }

            // ---------- 按钮交互 ----------
            // 重选模板：滚动到左侧并高亮当前，实际可以再次触发选中第一个，或者简单提示
            document.getElementById('reselectTemplate').addEventListener('click', function() {
                // 滚动左侧视图（简单模拟，直接选中第一个模板）
                if (templateItems.length > 0) {
                    setActiveTemplate(templateItems[0]);
                    templateItems[0].scrollIntoView({ behavior: 'smooth', block: 'nearest' });
                }
            });

            // 新建按钮
            document.getElementById('createProject').addEventListener('click', function() {
                const activeTemplate = document.querySelector('.template-item.active');
                const templateName = activeTemplate ? activeTemplate.querySelector('h3').innerText : '未知模板';
                const projectName = projectNameInput.value;
                const projectCode = projectCodeInput.value;
                const visibility = document.querySelector('.visibility-card.selected .title').innerText.trim();
                alert(`✅ 项目“${projectName}”准备创建！\n模板：${templateName}\n编号：${projectCode}\n可见性：${visibility}\n\n(演示模式，实际会提交表单)`);
            });

            // 模板管理按钮
            document.getElementById('templateManageBtn').addEventListener('click', function() {
                alert('模板管理：此处可跳转至模板配置页（演示）');
            });

            // 关闭按钮 (模拟关闭弹框，实际项目中可隐藏或移除)
            document.getElementById('closeModalBtn').addEventListener('click', function() {
                if (confirm('关闭创建窗口？未保存的内容将丢失。')) {
                    // 演示：直接移除弹框，或隐藏
                    document.querySelector('.project-modal').style.display = 'none';
                    // 可替换成实际关闭逻辑
                }
            });

            // 可选：文本域占位处理
            const textarea = document.querySelector('.textarea-field');
            textarea.addEventListener('focus', function() {
                if (this.value === '项目描述') {
                    this.value = '';
                }
            });
            textarea.addEventListener('blur', function() {
                if (this.value.trim() === '') {
                    this.value = '项目描述';
                }
            });
        })();
    </script>
</body>
</html>
```

### 主要改进说明
1. **左侧模板选择区**  
   - 完全替换了原封面上传区，展示5个模板卡片（经典项目管理、缺陷管理、业务反馈、产品规划、敏捷研发）。  
   - 每个卡片包含图标、标题、简短描述和模板标签，点击可选中并高亮。  
   - 顶部增加“模板管理”按钮（演示用）。

2. **右侧表单动态联动**  
   - 默认选中第一个模板，右侧项目名称、编号、描述自动填充对应模板的预设值（编号基于模板前缀+当前年月+随机数生成）。  
   - 切换左侧模板时，右侧内容实时更新。  
   - 工时统计仍保留4/100进度条（可根据需求独立定制）。

3. **右上角关闭按钮**  
   - 圆形关闭图标置于右侧白色区域右上角，悬浮于内容之上。  
   - 点击触发确认框，并隐藏整个弹框（演示效果，实际可替换为关闭逻辑）。

4. **交互保留**  
   - 公开性卡片切换、按钮点击反馈、文本域占位提示等均保持原风格。

该弹框完全基于您提供的图片描述优化，实现了“选择项目模板”与“填写项目信息”的左右联动，且右上角有关闭按钮。