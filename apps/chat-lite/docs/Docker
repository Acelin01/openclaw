# Docker 部署客户机器人 uxin

## 📋 方案说明

由于服务器 192.168.31.154 无法访问，我们使用 Docker 在本地创建一个虚拟的"远程服务器"环境。

## 🏗️ 架构对比

### 原方案（服务器离线）
```
你的电脑 → FRP 隧道 → 服务器 192.168.31.154 → 客户机器人 uxin
                        ❌ 离线
```

### 新方案（Docker 部署）
```
你的电脑 → FRP 隧道 → Docker 容器 → 客户机器人 uxin
                    ✅ 本地运行
```

## 🚀 快速启动

### 前提条件
1. 安装 Docker Desktop for Mac
   - 下载地址：https://www.docker.com/products/docker-desktop
   - 安装后启动 Docker Desktop

### 启动步骤

```bash
# 1. 进入 chat-lite 目录
cd /Users/mac/Documents/GitHub/openclaw/apps/chat-lite

# 2. 启动 Docker 容器
bash scripts/start-uxin-docker.sh

# 或手动执行
docker-compose -f docker-compose-uxin.yml up -d
```

### 验证服务

```bash
# 查看容器状态
docker-compose -f docker-compose-uxin.yml ps

# 查看日志
docker-compose -f docker-compose-uxin.yml logs -f

# 测试端口
nc -zv 127.0.0.1 7000   # FRP 服务器
nc -zv 127.0.0.1 60123  # FRP 隧道
nc -zv 127.0.0.1 18789  # Gateway
```

## 📊 服务信息

| 服务 | 地址 | 说明 |
|------|------|------|
| FRP 服务器 | 127.0.0.1:7000 | 接收 FRP 客户端连接 |
| FRP 隧道 | 127.0.0.1:60123 | 暴露给客户端的端口 |
| Gateway | 127.0.0.1:18789 | OpenClaw Gateway 服务 |
| Dashboard | http://127.0.0.1:7500 | FRP 管理界面 |

## 🔧 管理命令

```bash
# 查看状态
docker-compose -f docker-compose-uxin.yml ps

# 查看日志
docker-compose -f docker-compose-uxin.yml logs -f frps
docker-compose -f docker-compose-uxin.yml logs -f openclaw-gateway

# 重启服务
docker-compose -f docker-compose-uxin.yml restart

# 停止服务
docker-compose -f docker-compose-uxin.yml down

# 重新部署
docker-compose -f docker-compose-uxin.yml down
docker-compose -f docker-compose-uxin.yml up -d --build
```

## 📝 使用步骤

1. **启动 Docker 服务**
   ```bash
   bash scripts/start-uxin-docker.sh
   ```

2. **刷新浏览器**
   ```
   http://localhost:3003
   ```

3. **选择智能体**
   - 本地 Qwen 助手
   - 客户机器人 uxin (Docker) ← 选择这个

4. **发送消息测试**

## ⚠️ 注意事项

1. **资源占用**
   - Docker 容器会占用一定的 CPU 和内存
   - 约 500MB-1GB 内存
   - 约 2-5% CPU

2. **端口冲突**
   - 确保端口 7000、60123、18789 未被占用
   - 如有冲突，修改 docker-compose-uxin.yml

3. **网络配置**
   - 使用 host 网络模式，性能更好
   - 如需隔离，可改用 bridge 网络

## 🐛 故障排查

### 容器启动失败
```bash
# 查看详细日志
docker-compose -f docker-compose-uxin.yml logs

# 检查端口占用
lsof -i :7000
lsof -i :60123
lsof -i :18789
```

### 无法连接
```bash
# 测试 FRP 服务器
nc -zv 127.0.0.1 7000

# 测试隧道端口
nc -zv 127.0.0.1 60123

# 重启容器
docker-compose -f docker-compose-uxin.yml restart
```

### Gateway 未启动
```bash
# 查看 Gateway 日志
docker-compose -f docker-compose-uxin.yml logs openclaw-gateway

# 进入容器检查
docker exec -it uxin-gateway sh
```

## 📁 文件位置

| 文件 | 位置 |
|------|------|
| Docker Compose | docker-compose-uxin.yml |
| FRP 配置 | docker/frps.ini |
| 启动脚本 | scripts/start-uxin-docker.sh |
| 智能体配置 | config/agents.config.json |
| 日志 | docker-compose logs |

## 🎯 优势

✅ 不需要物理服务器
✅ 本地运行，延迟低
✅ 易于部署和管理
✅ 可随时启停
✅ 资源占用可控
✅ 与真实服务器环境一致
