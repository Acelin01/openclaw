```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>新建需求 - 移动端深度优化</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Noto+Sans+SC:wght@300;400;500;700&display=swap');

        :root {
            --green: #4CAF50;
            --green-light: #E8F5E9;
            --green-mid: #C8E6C9;
            --green-dark: #388E3C;
            --text-primary: #1a1a1a;
            --text-secondary: #555;
            --text-tertiary: #999;
            --border: #e8e8e8;
            --bg: #F4F6F8;
            --card: #ffffff;
            --radius: 14px;
            --radius-sm: 8px;
            --shadow: 0 2px 16px rgba(0,0,0,0.08);
            --shadow-lg: 0 8px 32px rgba(0,0,0,0.14);
            --red: #F44336;
            --blue: #2196F3;
            --orange: #FF9800;
            --safe-bottom: env(safe-area-inset-bottom, 0px);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            -webkit-tap-highlight-color: transparent;
        }

        html, body {
            font-family: 'Noto Sans SC', -apple-system, 'PingFang SC', 'Hiragino Sans GB', sans-serif;
            background: var(--bg);
            color: var(--text-primary);
            height: 100%;
            overflow-x: hidden;
        }

        /* DEMO PAGE */
        .demo-page {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 24px;
            background: linear-gradient(135deg, #e8f5e9 0%, #f1f8e9 50%, #f4f6f8 100%);
        }

        .demo-logo {
            width: 56px;
            height: 56px;
            border-radius: 16px;
            background: var(--green);
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 16px;
            box-shadow: 0 4px 20px rgba(76,175,80,0.3);
        }

        .demo-title {
            font-size: 22px;
            font-weight: 700;
            color: var(--text-primary);
            margin-bottom: 6px;
            text-align: center;
        }

        .demo-sub {
            font-size: 13px;
            color: var(--text-secondary);
            text-align: center;
            margin-bottom: 36px;
            line-height: 1.7;
        }

        .demo-btn {
            width: 100%;
            max-width: 320px;
            padding: 16px;
            background: var(--green);
            color: white;
            border: none;
            border-radius: var(--radius);
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            box-shadow: 0 4px 20px rgba(76,175,80,0.3);
            transition: all 0.2s;
            font-family: inherit;
        }

        .demo-btn:active {
            transform: scale(0.98);
            box-shadow: 0 2px 12px rgba(76,175,80,0.25);
        }

        /* DRAWER */
        .drawer-overlay {
            display: none;
            position: fixed;
            inset: 0;
            background: rgba(0,0,0,0.5);
            z-index: 100;
            backdrop-filter: blur(2px);
            animation: fadeIn 0.22s ease;
        }

        @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }

        .drawer-overlay.active { display: block; }

        .drawer {
            position: fixed;
            left: 0;
            right: 0;
            bottom: 0;
            top: 0;
            background: var(--bg);
            z-index: 101;
            display: flex;
            flex-direction: column;
            transform: translateY(100%);
            transition: transform 0.35s cubic-bezier(0.32, 0.72, 0, 1);
            will-change: transform;
        }

        .drawer.active { transform: translateY(0); }

        /* DRAWER HEADER */
        .drawer-header {
            background: var(--card);
            padding: 0 16px;
            border-bottom: 1px solid var(--border);
            flex-shrink: 0;
            position: sticky;
            top: 0;
            z-index: 30;
        }

        .drawer-header-top {
            display: flex;
            align-items: center;
            justify-content: space-between;
            height: 52px;
        }

        .drawer-close {
            width: 36px;
            height: 36px;
            border: none;
            background: var(--bg);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            color: var(--text-secondary);
        }

        .drawer-header-title { font-size: 17px; font-weight: 600; }

        .drawer-header-btn {
            padding: 8px 14px;
            background: var(--green);
            color: white;
            border: none;
            border-radius: 20px;
            font-size: 14px;
            font-weight: 500;
            cursor: pointer;
        }

        /* SEGMENT TABS */
        .segment-bar {
            display: flex;
            background: #f0f0f0;
            border-radius: 9px;
            padding: 3px;
            margin: 10px 0;
        }

        .segment-btn {
            flex: 1;
            padding: 6px;
            border: none;
            background: transparent;
            border-radius: 7px;
            font-size: 13px;
            color: var(--text-secondary);
            cursor: pointer;
            font-weight: 500;
        }

        .segment-btn.active {
            background: white;
            color: var(--text-primary);
            box-shadow: 0 1px 4px rgba(0,0,0,0.12);
        }

        /* SCROLL BODY */
        .drawer-body {
            flex: 1;
            overflow-y: auto;
            -webkit-overflow-scrolling: touch;
            overscroll-behavior: contain;
        }

        /* SECTION CARD */
        .section-card {
            background: var(--card);
            margin: 12px;
            border-radius: var(--radius);
            overflow: hidden;
            box-shadow: var(--shadow);
        }

        /* ===== 1. 父项输入框区域 ===== */
        .parent-container {
            margin-bottom: 10px;
        }
        .parent-chip {
            display: inline-flex;
            align-items: center;
            gap: 6px;
            padding: 5px 12px;
            border: 1.5px dashed var(--border);
            border-radius: 20px;
            background: transparent;
            font-size: 13px;
            color: var(--text-tertiary);
            cursor: pointer;
        }
        .parent-chip.has-parent {
            border-color: var(--green);
            background: var(--green-light);
            color: var(--green-dark);
        }
        /* 父项输入框（插入模式） */
        .parent-input-wrapper {
            display: none;
            align-items: center;
            gap: 8px;
            margin-bottom: 12px;
        }
        .parent-input-wrapper.active { display: flex; }
        .parent-input-field {
            position: relative;
            flex: 1;
        }
        .parent-input {
            width: 100%;
            padding: 12px 32px 12px 12px;
            border: 1.5px solid var(--green);
            border-radius: var(--radius-sm);
            font-size: 14px;
            outline: none;
            background: white;
        }
        .parent-clear-icon {
            position: absolute;
            right: 10px;
            top: 50%;
            transform: translateY(-50%);
            color: var(--text-tertiary);
            cursor: pointer;
        }
        .parent-cancel-btn {
            padding: 10px 14px;
            background: var(--bg);
            border: 1px solid var(--border);
            border-radius: var(--radius-sm);
            font-size: 13px;
            color: var(--text-secondary);
            cursor: pointer;
            white-space: nowrap;
        }
        /* 已选父项展示 */
        .parent-selected {
            display: none;
            align-items: center;
            justify-content: space-between;
            background: var(--green-light);
            padding: 12px 14px;
            border-radius: var(--radius-sm);
            margin-top: 8px;
        }
        .parent-selected.active { display: flex; }
        .parent-selected-info {
            display: flex;
            flex-direction: column;
            gap: 4px;
        }
        .parent-selected-title {
            font-size: 14px;
            font-weight: 500;
            color: var(--text-primary);
            display: flex;
            align-items: center;
            gap: 6px;
        }
        .parent-id {
            color: var(--green-dark);
            font-weight: 600;
        }
        .parent-sub-count {
            background: rgba(76,175,80,0.15);
            padding: 2px 6px;
            border-radius: 12px;
            font-size: 11px;
            color: var(--green-dark);
            cursor: pointer;
        }
        .parent-selected-meta {
            font-size: 11px;
            color: var(--text-secondary);
        }
        .parent-more-btn {
            background: transparent;
            border: none;
            color: var(--text-tertiary);
            padding: 8px;
            cursor: pointer;
        }

        /* TITLE */
        .title-input {
            width: 100%;
            border: none;
            font-size: 19px;
            font-weight: 700;
            color: var(--text-primary);
            outline: none;
            line-height: 1.4;
            background: transparent;
            resize: none;
        }
        .title-input::placeholder { color: var(--text-tertiary); }
        .char-count {
            font-size: 11px;
            color: var(--text-tertiary);
            text-align: right;
            margin-top: 6px;
        }

        /* ===== 2. 编辑器及置顶工具栏 ===== */
        .editor-container {
            border-top: 1px solid var(--border);
        }
        .desc-toolbar {
            display: flex;
            gap: 2px;
            padding: 8px 12px;
            overflow-x: auto;
            scrollbar-width: none;
            border-bottom: 1px solid var(--border);
            background: #fafafa;
            position: sticky;  /* 置顶固定 */
            top: 0;
            z-index: 20;
            transition: box-shadow 0.2s;
        }
        .desc-toolbar.stuck {
            box-shadow: 0 2px 8px rgba(0,0,0,0.08);
        }
        .desc-toolbar::-webkit-scrollbar { display: none; }
        .toolbar-btn {
            padding: 6px 10px;
            border: none;
            background: transparent;
            border-radius: 6px;
            font-size: 14px;
            color: var(--text-secondary);
            cursor: pointer;
            white-space: nowrap;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-shrink: 0;
        }
        .toolbar-sep {
            width: 1px;
            background: var(--border);
            margin: 4px 4px;
            align-self: stretch;
        }
        .desc-input {
            width: 100%;
            border: none;
            outline: none;
            padding: 14px 16px;
            font-size: 14px;
            line-height: 1.7;
            color: var(--text-primary);
            background: transparent;
            resize: none;
            min-height: 120px;
        }

        /* ===== 3. 关联项模块（横向滚动表格 + 固定右侧操作栏） ===== */
        .assoc-wrapper {
            margin: 12px 16px 0;
        }
        .assoc-header-row {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 12px;
        }
        .assoc-tabs-scroll {
            display: flex;
            gap: 4px;
            overflow-x: auto;
            flex: 1;
            padding-right: 10px;
        }
        .assoc-tab {
            padding: 5px 12px;
            border-radius: 16px;
            border: none;
            background: var(--bg);
            font-size: 12px;
            color: var(--text-secondary);
            cursor: pointer;
            white-space: nowrap;
            font-weight: 500;
            flex-shrink: 0;
        }
        .assoc-tab.active {
            background: var(--green);
            color: white;
        }
        .count-badge {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            min-width: 16px;
            height: 16px;
            border-radius: 8px;
            background: rgba(255,255,255,0.3);
            font-size: 10px;
            padding: 0 4px;
            margin-left: 4px;
        }
        .assoc-add-btn {
            padding: 5px 12px;
            background: var(--green-light);
            color: var(--green-dark);
            border: none;
            border-radius: 16px;
            font-size: 12px;
            font-weight: 600;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 4px;
            flex-shrink: 0;
        }

        /* 关联搜索输入框（触发弹框） */
        .assoc-search {
            display: flex;
            align-items: center;
            gap: 8px;
            margin: 12px 16px;
            padding: 10px 14px;
            background: var(--bg);
            border-radius: var(--radius-sm);
            border: 1.5px solid var(--border);
        }
        .assoc-search input {
            flex: 1;
            border: none;
            background: transparent;
            font-size: 14px;
            outline: none;
        }
        .assoc-search .cancel-text {
            font-size: 13px;
            color: var(--text-secondary);
            cursor: pointer;
        }

        /* 关联表格 + 固定右侧操作栏 */
        .assoc-table-container {
            display: flex;
            align-items: flex-start;
            margin: 0 16px 12px;
            background: var(--card);
            border-radius: var(--radius-sm);
            border: 1px solid var(--border);
            overflow: hidden;
        }
        .assoc-scroll {
            flex: 1;
            overflow-x: auto;
            -webkit-overflow-scrolling: touch;
        }
        .assoc-table {
            width: 100%;
            border-collapse: collapse;
            min-width: 550px;
            font-size: 12px;
        }
        .assoc-table th {
            background: #fafafa;
            padding: 12px 8px;
            font-weight: 600;
            color: var(--text-secondary);
            border-bottom: 1px solid var(--border);
            white-space: nowrap;
        }
        .assoc-table td {
            padding: 12px 8px;
            border-bottom: 1px solid var(--border);
            white-space: nowrap;
        }
        .assoc-table tr:last-child td { border-bottom: none; }
        .demand-badge {
            display: inline-block;
            width: 20px;
            height: 20px;
            border-radius: 4px;
            background: var(--green-light);
            color: var(--green);
            text-align: center;
            line-height: 20px;
            margin-right: 6px;
        }

        .assoc-actions-side {
            flex-shrink: 0;
            width: 60px;
            background: #fafafa;
            border-left: 1px solid var(--border);
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 12px 0;
            gap: 16px;
        }
        .action-icon {
            color: var(--text-tertiary);
            cursor: pointer;
            padding: 8px;
            border-radius: 4px;
        }
        .action-icon:active { background: var(--border); }

        /* ===== 4. 基础字段 鼠标/触摸移入效果 ===== */
        .field-row {
            display: flex;
            align-items: center;
            padding: 14px 16px;
            border-top: 1px solid var(--border);
            cursor: pointer;
            transition: background 0.15s, border 0.15s;
            border: 1px solid transparent; /* 用于 hover 效果 */
            margin: -1px 0; /* 防止抖动 */
        }
        .field-row:active {
            background: var(--bg);
            border-color: var(--green);
            border-radius: var(--radius-sm);
        }
        .field-row-label {
            font-size: 14px;
            color: var(--text-secondary);
            width: 90px;
            flex-shrink: 0;
        }
        .required-dot {
            width: 5px;
            height: 5px;
            border-radius: 50%;
            background: var(--red);
            display: inline-block;
            margin-right: 3px;
        }
        .field-row-value {
            flex: 1;
            font-size: 14px;
            color: var(--text-primary);
            display: flex;
            align-items: center;
            gap: 6px;
        }
        .field-row-value.placeholder { color: var(--text-tertiary); }
        .field-row-arrow { color: var(--text-tertiary); }

        /* 预计工时特殊样式 */
        .hours-display {
            display: flex;
            align-items: center;
            gap: 6px;
        }
        .hours-icon {
            width: 20px;
            height: 20px;
            border-radius: 5px;
            background: var(--green-light);
            color: var(--green);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 11px;
        }

        /* 优先级徽章 */
        .priority-pill {
            display: inline-flex;
            align-items: center;
            gap: 5px;
            padding: 3px 10px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
        }
        .priority-high { background: #FFEBEE; color: #C62828; }
        .priority-medium { background: #FFF3E0; color: #E65100; }
        .priority-low { background: var(--green-light); color: var(--green-dark); }
        .priority-dot {
            width: 6px;
            height: 6px;
            border-radius: 50%;
        }
        .ph-high { background: #EF5350; }
        .ph-medium { background: #FFA726; }
        .ph-low { background: var(--green); }

        .status-pill {
            display: inline-flex;
            padding: 3px 10px;
            border-radius: 20px;
            font-size: 12px;
            background: var(--green-light);
            color: var(--green-dark);
        }
        .avatar-sm {
            width: 22px;
            height: 22px;
            border-radius: 50%;
            background: var(--green);
            color: white;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            font-size: 10px;
            font-weight: 700;
        }

        /* ATTACHMENT */
        .attach-section { padding: 14px 16px; border-top: 1px solid var(--border); }
        .attach-label {
            font-size: 12px;
            font-weight: 600;
            color: var(--text-tertiary);
            text-transform: uppercase;
            margin-bottom: 10px;
        }
        .attach-row {
            display: flex;
            gap: 10px;
            overflow-x: auto;
            padding-bottom: 4px;
        }
        .attach-add {
            width: 64px;
            height: 64px;
            border-radius: 10px;
            border: 1.5px dashed var(--border);
            background: transparent;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            gap: 4px;
            cursor: pointer;
            color: var(--text-tertiary);
            font-size: 10px;
            flex-shrink: 0;
        }

        /* FOOTER */
        .drawer-footer {
            background: var(--card);
            border-top: 1px solid var(--border);
            padding: 12px 16px;
            padding-bottom: calc(12px + var(--safe-bottom));
            flex-shrink: 0;
        }
        .footer-row {
            display: flex;
            align-items: center;
            gap: 10px;
        }
        .toggle-label {
            display: flex;
            align-items: center;
            gap: 8px;
            font-size: 13px;
            color: var(--text-secondary);
            cursor: pointer;
        }
        .toggle-switch {
            width: 40px;
            height: 22px;
            border-radius: 11px;
            background: var(--border);
            position: relative;
            transition: background 0.2s;
        }
        .toggle-switch.on { background: var(--green); }
        .toggle-thumb {
            position: absolute;
            top: 2px;
            left: 2px;
            width: 18px;
            height: 18px;
            border-radius: 50%;
            background: white;
            box-shadow: 0 1px 4px rgba(0,0,0,0.2);
            transition: left 0.2s;
        }
        .toggle-switch.on .toggle-thumb { left: 20px; }
        .footer-submit {
            flex: 1;
            padding: 13px;
            background: var(--green);
            color: white;
            border: none;
            border-radius: var(--radius-sm);
            font-size: 15px;
            font-weight: 600;
            cursor: pointer;
        }

        /* BOTTOM SHEETS */
        .sheet-overlay {
            display: none;
            position: fixed;
            inset: 0;
            background: rgba(0,0,0,0.5);
            z-index: 200;
            animation: fadeIn 0.2s ease;
        }
        .sheet-overlay.active { display: block; }
        .bottom-sheet {
            position: fixed;
            left: 0;
            right: 0;
            bottom: 0;
            background: var(--card);
            border-radius: 20px 20px 0 0;
            z-index: 201;
            transform: translateY(100%);
            transition: transform 0.3s cubic-bezier(0.32, 0.72, 0, 1);
            max-height: 70vh;
            display: flex;
            flex-direction: column;
            padding-bottom: var(--safe-bottom);
        }
        .bottom-sheet.active { transform: translateY(0); }
        .sheet-handle {
            width: 36px;
            height: 4px;
            border-radius: 2px;
            background: var(--border);
            margin: 10px auto 0;
        }
        .sheet-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 16px 20px 10px;
        }
        .sheet-title { font-size: 16px; font-weight: 700; }
        .sheet-cancel-btn {
            font-size: 14px;
            color: var(--text-secondary);
            cursor: pointer;
            background: none;
            border: none;
        }
        .sheet-body { overflow-y: auto; }
        .sheet-option {
            display: flex;
            align-items: center;
            padding: 15px 20px;
            border-bottom: 1px solid var(--border);
            cursor: pointer;
            font-size: 15px;
            color: var(--text-primary);
            gap: 12px;
        }
        .sheet-option.selected { color: var(--green); font-weight: 600; }
        .check-icon { margin-left: auto; color: var(--green); opacity: 0; }
        .sheet-option.selected .check-icon { opacity: 1; }

        /* 子项列表弹框 (简单) */
        .subitem-sheet .sheet-body { padding: 12px 20px; }
        .subitem-item {
            padding: 12px 0;
            border-bottom: 1px solid var(--border);
        }
        .subitem-title { font-weight: 500; margin-bottom: 4px; }
        .subitem-meta { font-size: 12px; color: var(--text-tertiary); }

        /* TOAST */
        .toast {
            position: fixed;
            bottom: 100px;
            left: 50%;
            transform: translateX(-50%) translateY(20px);
            background: rgba(0,0,0,0.75);
            color: white;
            padding: 10px 20px;
            border-radius: 20px;
            font-size: 14px;
            opacity: 0;
            transition: all 0.3s;
            z-index: 999;
            white-space: nowrap;
            pointer-events: none;
        }
        .toast.show { opacity: 1; transform: translateX(-50%) translateY(0); }
    </style>
</head>
<body>

    <!-- DEMO 启动页 -->
    <div class="demo-page" id="demoPage">
        <div class="demo-logo">
            <svg width="28" height="28" viewBox="0 0 28 28" fill="none">
                <path d="M14 5V23M5 14H23" stroke="white" stroke-width="2.5" stroke-linecap="round"/>
            </svg>
        </div>
        <h1 class="demo-title">云效 Project</h1>
        <p class="demo-sub">移动端 · 深度优化<br>完全满足5项交互要求</p>
        <button class="demo-btn" onclick="openDrawer()">
            <svg width="18" height="18" viewBox="0 0 18 18" fill="none">
                <path d="M9 3V15M3 9H15" stroke="white" stroke-width="2" stroke-linecap="round"/>
            </svg>
            新建需求
        </button>
    </div>

    <!-- ========== 主抽屉 ========== -->
    <div class="drawer-overlay" id="drawerOverlay" onclick="closeDrawer()"></div>
    <div class="drawer" id="mainDrawer">
        <div class="drawer-header">
            <div class="drawer-header-top">
                <button class="drawer-close" onclick="closeDrawer()">
                    <svg width="18" height="18" viewBox="0 0 18 18" fill="none">
                        <path d="M4 4L14 14M4 14L14 4" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/>
                    </svg>
                </button>
                <span class="drawer-header-title">新建需求</span>
                <button class="drawer-header-btn" onclick="createDemand()">新建</button>
            </div>
            <div class="segment-bar">
                <button class="segment-btn active" onclick="switchTab(this, 'tab-basic')">基础信息</button>
                <button class="segment-btn" onclick="switchTab(this, 'tab-related')">关联项</button>
                <button class="segment-btn" onclick="switchTab(this, 'tab-fields')">字段设置</button>
            </div>
        </div>

        <div class="drawer-body" id="drawerBody">

            <!-- ===== TAB 1: 基础信息 ===== -->
            <div id="tab-basic">
                <div class="section-card">
                    <div class="title-area" style="padding:16px;">
                        <!-- 父项容器：芯片 + 输入框 + 已选展示 -->
                        <div class="parent-container">
                            <!-- 芯片按钮（插入父项） -->
                            <button class="parent-chip" id="parentChip" onclick="showParentInput()">
                                <svg width="12" height="12" viewBox="0 0 12 12" fill="none">
                                    <path d="M6 1V11M1 6H11" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
                                </svg>
                                <span id="parentChipText">插入父项</span>
                            </button>
                            <!-- 父项输入框（含清除icon + 取消） -->
                            <div class="parent-input-wrapper" id="parentInputWrapper">
                                <div class="parent-input-field">
                                    <input type="text" class="parent-input" id="parentSearchField" placeholder="请输入标题或ID以关联" autocomplete="off">
                                    <span class="parent-clear-icon" onclick="clearParentInput()">
                                        <svg width="14" height="14" viewBox="0 0 14 14" fill="none">
                                            <path d="M2 2L12 12M2 12L12 2" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
                                        </svg>
                                    </span>
                                </div>
                                <button class="parent-cancel-btn" onclick="hideParentInput()">取消</button>
                            </div>
                            <!-- 已选父项展示 -->
                            <div class="parent-selected" id="parentSelected">
                                <div class="parent-selected-info">
                                    <div class="parent-selected-title">
                                        <span class="parent-id" id="selectedParentId">DEMO-26</span>
                                        <span id="selectedParentTitle">云效Project-新一代研发协作平台发布！</span>
                                        <span class="parent-sub-count" id="parentSubCount" onclick="openSubitemSheet()">2</span>
                                    </div>
                                    <div class="parent-selected-meta" id="selectedParentMeta">
                                        状态: 尚未提交 · 负责人: lenny540101@163.com
                                    </div>
                                </div>
                                <div style="position:relative;">
                                    <button class="parent-more-btn" onclick="toggleParentMenu()">
                                        <svg width="18" height="18" viewBox="0 0 18 18" fill="none">
                                            <circle cx="9" cy="5" r="1.5" fill="currentColor"/>
                                            <circle cx="9" cy="9" r="1.5" fill="currentColor"/>
                                            <circle cx="9" cy="13" r="1.5" fill="currentColor"/>
                                        </svg>
                                    </button>
                                    <!-- 更多操作菜单 (简单) -->
                                    <div id="parentMoreMenu" style="display:none; position:absolute; right:0; top:30px; background:white; border:1px solid var(--border); border-radius:8px; z-index:50;">
                                        <div style="padding:12px 16px;" onclick="changeParent()">更换父项</div>
                                        <div style="padding:12px 16px; color:var(--red);" onclick="showRemoveConfirm()">移除父项</div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- 需求标题 -->
                        <textarea class="title-input" id="demandTitle" placeholder="请输入需求标题" rows="2" maxlength="200" oninput="updateCharCount(this)">云效Project-新一代研发协作平台发布！</textarea>
                        <div class="char-count"><span id="charCount">18</span>/200</div>
                    </div>

                    <!-- 编辑器（带置顶工具栏） -->
                    <div class="editor-container">
                        <div class="desc-toolbar" id="editorToolbar">
                            <button class="toolbar-btn" onclick="execCmd('undo')">↶</button>
                            <button class="toolbar-btn" onclick="execCmd('redo')">↷</button>
                            <div class="toolbar-sep"></div>
                            <button class="toolbar-btn" onclick="execCmd('bold')"><b>B</b></button>
                            <button class="toolbar-btn" onclick="execCmd('italic')"><i>I</i></button>
                            <button class="toolbar-btn" onclick="execCmd('underline')"><u>U</u></button>
                            <div class="toolbar-sep"></div>
                            <button class="toolbar-btn" onclick="execCmd('insertOrderedList')">1,2</button>
                            <button class="toolbar-btn" onclick="execCmd('insertUnorderedList')">•</button>
                            <div class="toolbar-sep"></div>
                            <button class="toolbar-btn" onclick="insertLink()">🔗</button>
                            <button class="toolbar-btn" onclick="insertImage()">🖼</button>
                        </div>
                        <div id="descEditor" contenteditable="true" class="desc-input" 
                             data-placeholder="请简单描述一下做了什么"
                             oninput="handleEditorInput(this)">
                            云效Project是一个新一代的研发协作平台，旨在提供全方位的研发管理解决方案。
                        </div>
                    </div>
                </div>

                <!-- 附件卡片 -->
                <div class="section-card">
                    <div class="attach-section">
                        <div class="attach-label">📎 附件</div>
                        <div class="attach-row" id="attachRow">
                            <button class="attach-add" onclick="showToast('附件上传开发中')">
                                <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
                                    <path d="M10 4v12M4 10h12" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/>
                                </svg>
                                上传
                            </button>
                        </div>
                    </div>
                </div>
            </div>

            <!-- ===== TAB 2: 关联项 ===== -->
            <div id="tab-related" style="display:none">
                <div class="section-card" style="padding-bottom:16px;">
                    <!-- 选项卡 + 右侧关联按钮 -->
                    <div class="assoc-wrapper">
                        <div class="assoc-header-row">
                            <div class="assoc-tabs-scroll">
                                <button class="assoc-tab active" onclick="filterAssoc(this,'all')">全部 <span class="count-badge">2</span></button>
                                <button class="assoc-tab" onclick="filterAssoc(this,'demand')">需求 <span class="count-badge">2</span></button>
                                <button class="assoc-tab" onclick="filterAssoc(this,'task')">任务 <span class="count-badge">0</span></button>
                                <button class="assoc-tab" onclick="filterAssoc(this,'defect')">缺陷 <span class="count-badge">0</span></button>
                                <button class="assoc-tab" onclick="filterAssoc(this,'risk')">风险 <span class="count-badge">0</span></button>
                            </div>
                            <button class="assoc-add-btn" onclick="openAssocSheet()">
                                <svg width="12" height="12" viewBox="0 0 12 12" fill="none"><path d="M6 1v10M1 6h10" stroke="currentColor" stroke-width="1.5"/></svg>
                                关联
                            </button>
                        </div>
                    </div>

                    <!-- 关联输入框（前置搜索icon + 后置取消） -->
                    <div class="assoc-search" onclick="openAssocSheet()">
                        <svg width="15" height="15" viewBox="0 0 15 15" fill="none">
                            <circle cx="6.5" cy="6.5" r="4.5" stroke="#999" stroke-width="1.3"/>
                            <path d="M10 10L13 13" stroke="#999" stroke-width="1.3" stroke-linecap="round"/>
                        </svg>
                        <input type="text" id="assocSearchInput" placeholder="搜索标题或ID" readonly onfocus="openAssocSheet()">
                        <span class="cancel-text" onclick="event.stopPropagation();clearAssocSearch()">取消</span>
                    </div>

                    <!-- 关联列表：横向滑动表格 + 固定右侧操作栏 -->
                    <div class="assoc-table-container">
                        <div class="assoc-scroll">
                            <table class="assoc-table" id="assocTable">
                                <thead>
                                    <tr>
                                        <th>编号</th>
                                        <th>标题</th>
                                        <th>状态</th>
                                        <th>优先级</th>
                                        <th>创建人</th>
                                    </tr>
                                </thead>
                                <tbody id="assocTableBody">
                                    <tr data-type="demand">
                                        <td><span class="demand-badge">◎</span>DEMO-26</td>
                                        <td>云效Project-新一代研发协作平台发布！</td>
                                        <td><span class="status-pill">尚未提交</span></td>
                                        <td><span class="priority-pill priority-high"><span class="priority-dot ph-high"></span>高</span></td>
                                        <td><span class="avatar-sm">LE</span>lenny54...</td>
                                    </tr>
                                    <tr data-type="demand">
                                        <td><span class="demand-badge">◎</span>DEMO-25</td>
                                        <td>如何选择合适的项目模板？</td>
                                        <td><span class="status-pill">尚未提交</span></td>
                                        <td><span class="priority-pill priority-high"><span class="priority-dot ph-high"></span>高</span></td>
                                        <td><span class="avatar-sm">LE</span>lenny54...</td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                        <!-- 固定右侧操作栏（竖向） -->
                        <div class="assoc-actions-side">
                            <div class="action-icon" title="标题设置" onclick="showToast('标题设置')">
                                <svg width="18" height="18" viewBox="0 0 18 18" fill="none">
                                    <rect x="2" y="2" width="14" height="14" rx="2" stroke="currentColor" stroke-width="1.5"/>
                                    <path d="M5 5H13M5 9H13M5 13H10" stroke="currentColor" stroke-width="1.5"/>
                                </svg>
                            </div>
                            <div class="action-icon" title="解绑" onclick="showToast('解绑关联项')">
                                <svg width="18" height="18" viewBox="0 0 18 18" fill="none">
                                    <circle cx="9" cy="9" r="6" stroke="currentColor" stroke-width="1.5"/>
                                    <path d="M6 6L12 12M6 12L12 6" stroke="currentColor" stroke-width="1.5"/>
                                </svg>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- ===== TAB 3: 字段设置 ===== -->
            <div id="tab-fields" style="display:none">
                <div class="section-card">
                    <div class="fields-header"><span class="fields-label">基础字段</span></div>
                    <div class="field-row" onclick="openSheet('workTypeSheet')">
                        <div class="field-row-label"><span class="required-dot"></span>工作项类型</div>
                        <div class="field-row-value" id="workTypeVal">产品类需求</div>
                        <svg class="field-row-arrow" width="16" height="16" viewBox="0 0 16 16"><path d="M6 4l4 4-4 4" stroke="currentColor" stroke-width="1.5"/></svg>
                    </div>
                    <div class="field-row" onclick="openSheet('assigneeSheet')">
                        <div class="field-row-label"><span class="required-dot"></span>负责人</div>
                        <div class="field-row-value" id="assigneeVal"><span class="avatar-sm">LE</span>lenny540101</div>
                        <svg class="field-row-arrow" width="16" height="16"><path d="M6 4l4 4-4 4" stroke="currentColor" stroke-width="1.5"/></svg>
                    </div>
                    <div class="field-row" onclick="openSheet('prioritySheet')">
                        <div class="field-row-label"><span class="required-dot"></span>优先级</div>
                        <div class="field-row-value" id="priorityVal"><span class="priority-pill priority-high"><span class="priority-dot ph-high"></span>高</span></div>
                        <svg class="field-row-arrow" width="16" height="16"><path d="M6 4l4 4-4 4" stroke="currentColor" stroke-width="1.5"/></svg>
                    </div>
                    <div class="field-row" onclick="openSheet('projectSheet')">
                        <div class="field-row-label"><span class="required-dot"></span>归属项目</div>
                        <div class="field-row-value" id="projectVal">敏捷研发示例项目</div>
                        <svg class="field-row-arrow" width="16" height="16"><path d="M6 4l4 4-4 4" stroke="currentColor" stroke-width="1.5"/></svg>
                    </div>
                    <div class="field-row" onclick="openSheet('iterationSheet')">
                        <div class="field-row-label">迭代</div>
                        <div class="field-row-value placeholder" id="iterationVal">请选择</div>
                        <svg class="field-row-arrow" width="16" height="16"><path d="M6 4l4 4-4 4" stroke="currentColor" stroke-width="1.5"/></svg>
                    </div>
                    <div class="field-row" onclick="openSheet('participantSheet')">
                        <div class="field-row-label">参与者</div>
                        <div class="field-row-value placeholder" id="participantVal">请选择</div>
                        <svg class="field-row-arrow" width="16" height="16"><path d="M6 4l4 4-4 4" stroke="currentColor" stroke-width="1.5"/></svg>
                    </div>
                    <div class="field-row" onclick="openSheet('tagSheet')">
                        <div class="field-row-label">标签</div>
                        <div class="field-row-value placeholder" id="tagVal">请选择</div>
                        <svg class="field-row-arrow" width="16" height="16"><path d="M6 4l4 4-4 4" stroke="currentColor" stroke-width="1.5"/></svg>
                    </div>
                    <div class="field-row" onclick="openDatePicker('startDate')">
                        <div class="field-row-label">计划开始</div>
                        <div class="field-row-value placeholder" id="startDateVal">请选择日期</div>
                        <svg class="field-row-arrow" width="16" height="16"><path d="M6 4l4 4-4 4" stroke="currentColor" stroke-width="1.5"/></svg>
                    </div>
                    <div class="field-row" onclick="openDatePicker('endDate')">
                        <div class="field-row-label">计划完成</div>
                        <div class="field-row-value placeholder" id="endDateVal">请选择日期</div>
                        <svg class="field-row-arrow" width="16" height="16"><path d="M6 4l4 4-4 4" stroke="currentColor" stroke-width="1.5"/></svg>
                    </div>

                    <div class="fields-header"><span class="fields-label">⏱ 工时</span></div>
                    <div class="field-row" onclick="openHoursSheet()">
                        <div class="field-row-label">预计工时</div>
                        <div class="field-row-value" id="hoursVal">
                            <div class="hours-display"><div class="hours-icon">⏱</div><span id="hoursText">预估</span></div>
                        </div>
                        <svg class="field-row-arrow" width="16" height="16"><path d="M6 4l4 4-4 4" stroke="currentColor" stroke-width="1.5"/></svg>
                    </div>
                </div>
            </div>
        </div>

        <!-- 底部 -->
        <div class="drawer-footer">
            <div class="footer-row">
                <label class="toggle-label" onclick="toggleContinue()">
                    <div class="toggle-switch" id="continueToggle"><div class="toggle-thumb"></div></div>
                    继续新建
                </label>
                <button class="footer-submit" onclick="createDemand()">新建需求</button>
            </div>
        </div>
    </div>

    <!-- ========== 底部弹框集合 ========== -->
    <!-- 父项搜索结果弹框（由父项输入框焦点触发） -->
    <div class="sheet-overlay" id="parentSearchSheetOverlay" onclick="closeSheet('parentSearchSheet')"></div>
    <div class="bottom-sheet" id="parentSearchSheet">
        <div class="sheet-handle"></div>
        <div class="sheet-header">
            <span class="sheet-title">选择父项</span>
            <button class="sheet-cancel-btn" onclick="closeSheet('parentSearchSheet')">关闭</button>
        </div>
        <div class="sheet-body">
            <div class="parent-search-sheet" style="padding:12px 16px;">
                <div class="parent-search-inner" style="display:flex; background:var(--bg); padding:10px; border-radius:8px;">
                    <input type="text" id="parentModalSearch" placeholder="请输入标题或ID" style="flex:1; border:none; background:transparent; outline:none;">
                </div>
            </div>
            <div id="parentModalResults">
                <div class="parent-result" style="padding:14px 20px; border-bottom:1px solid var(--border); cursor:pointer;" onclick="selectParentFromModal('DEMO-26','云效Project-新一代研发协作平台发布！',true)">
                    <div class="parent-result-title">云效Project-新一代研发协作平台发布！</div>
                    <div class="parent-result-meta">DEMO-26 · 尚未提交 · 子项2</div>
                </div>
                <div class="parent-result" style="padding:14px 20px; border-bottom:1px solid var(--border); cursor:pointer;" onclick="selectParentFromModal('DEMO-25','如何选择合适的项目模板？',false)">
                    <div class="parent-result-title">如何选择合适的项目模板？</div>
                    <div class="parent-result-meta">DEMO-25 · 尚未提交</div>
                </div>
            </div>
        </div>
    </div>

    <!-- 其他选择弹框（工作项类型、负责人、优先级、项目等） -->
    <div class="sheet-overlay" id="workTypeSheetOverlay" onclick="closeSheet('workTypeSheet')"></div>
    <div class="bottom-sheet" id="workTypeSheet"><div class="sheet-handle"></div><div class="sheet-header"><span class="sheet-title">工作项类型</span><button class="sheet-cancel-btn" onclick="closeSheet('workTypeSheet')">关闭</button></div><div class="sheet-body"><div class="sheet-option selected" onclick="selectOption('workTypeSheet','workTypeVal',this,'产品类需求')">产品类需求<span class="check-icon">✓</span></div><div class="sheet-option" onclick="selectOption('workTypeSheet','workTypeVal',this,'技术类需求')">技术类需求<span class="check-icon">✓</span></div><div class="sheet-option" onclick="selectOption('workTypeSheet','workTypeVal',this,'设计类需求')">设计类需求<span class="check-icon">✓</span></div></div></div>

    <div class="sheet-overlay" id="assigneeSheetOverlay" onclick="closeSheet('assigneeSheet')"></div>
    <div class="bottom-sheet" id="assigneeSheet"><div class="sheet-handle"></div><div class="sheet-header"><span class="sheet-title">负责人</span><button class="sheet-cancel-btn" onclick="closeSheet('assigneeSheet')">关闭</button></div><div class="sheet-body"><div class="sheet-option selected" onclick="selectAssignee(this,'lenny540101@163.com','LE')"><span class="avatar-sm">LE</span>lenny540101@163.com<span class="check-icon">✓</span></div><div class="sheet-option" onclick="selectAssignee(this,'zhangsan@163.com','张')"><span class="avatar-sm" style="background:#2196F3">张</span>zhangsan@163.com<span class="check-icon">✓</span></div></div></div>

    <div class="sheet-overlay" id="prioritySheetOverlay" onclick="closeSheet('prioritySheet')"></div>
    <div class="bottom-sheet" id="prioritySheet"><div class="sheet-handle"></div><div class="sheet-header"><span class="sheet-title">优先级</span><button class="sheet-cancel-btn" onclick="closeSheet('prioritySheet')">关闭</button></div><div class="sheet-body"><div class="sheet-option selected" onclick="selectPriority(this,'high','高')"><span class="priority-pill priority-high"><span class="priority-dot ph-high"></span>高</span><span class="check-icon">✓</span></div><div class="sheet-option" onclick="selectPriority(this,'medium','中')"><span class="priority-pill priority-medium"><span class="priority-dot ph-medium"></span>中</span><span class="check-icon">✓</span></div></div></div>

    <div class="sheet-overlay" id="projectSheetOverlay" onclick="closeSheet('projectSheet')"></div>
    <div class="bottom-sheet" id="projectSheet"><div class="sheet-handle"></div><div class="sheet-header"><span class="sheet-title">归属项目</span><button class="sheet-cancel-btn" onclick="closeSheet('projectSheet')">关闭</button></div><div class="sheet-body"><div class="sheet-option selected" onclick="selectOption('projectSheet','projectVal',this,'敏捷研发示例项目')">敏捷研发示例项目<span class="check-icon">✓</span></div><div class="sheet-option" onclick="selectOption('projectSheet','projectVal',this,'数据开发示例项目')">数据开发示例项目<span class="check-icon">✓</span></div></div></div>

    <div class="sheet-overlay" id="iterationSheetOverlay" onclick="closeSheet('iterationSheet')"></div>
    <div class="bottom-sheet" id="iterationSheet"><div class="sheet-handle"></div><div class="sheet-header"><span class="sheet-title">迭代</span><button class="sheet-cancel-btn" onclick="closeSheet('iterationSheet')">关闭</button></div><div class="sheet-body"><div class="sheet-option" onclick="selectOption('iterationSheet','iterationVal',this,'迭代2026.01')">迭代2026.01<span class="check-icon">✓</span></div></div></div>

    <div class="sheet-overlay" id="participantSheetOverlay" onclick="closeSheet('participantSheet')"></div>
    <div class="bottom-sheet" id="participantSheet"><div class="sheet-handle"></div><div class="sheet-header"><span class="sheet-title">参与者</span><button class="sheet-cancel-btn" onclick="closeSheet('participantSheet');saveMultiSelect('participantSheet','participantVal')">完成</button></div><div class="sheet-body"><div class="sheet-option" onclick="toggleMulti(this)"><span class="avatar-sm">LE</span>lenny540101<span class="check-icon">✓</span></div><div class="sheet-option" onclick="toggleMulti(this)"><span class="avatar-sm" style="background:#2196F3">张</span>zhangsan<span class="check-icon">✓</span></div></div></div>

    <div class="sheet-overlay" id="tagSheetOverlay" onclick="closeSheet('tagSheet')"></div>
    <div class="bottom-sheet" id="tagSheet"><div class="sheet-handle"></div><div class="sheet-header"><span class="sheet-title">标签</span><button class="sheet-cancel-btn" onclick="closeSheet('tagSheet');saveMultiSelect('tagSheet','tagVal')">完成</button></div><div class="sheet-body"><div class="sheet-option" onclick="toggleMulti(this)">工作台<span class="check-icon">✓</span></div><div class="sheet-option" onclick="toggleMulti(this)">个性化<span class="check-icon">✓</span></div></div></div>

    <!-- 工时设置 -->
    <div class="sheet-overlay" id="hoursSheetOverlay" onclick="closeSheet('hoursSheet')"></div>
    <div class="bottom-sheet" id="hoursSheet">
        <div class="sheet-handle"></div>
        <div class="sheet-header"><span class="sheet-title">预计工时</span><button class="sheet-cancel-btn" onclick="closeSheet('hoursSheet')">关闭</button></div>
        <div class="sheet-body">
            <div class="hours-sheet-content" style="padding:16px 20px;">
                <div class="hours-sheet-label">工时（小时）</div>
                <input type="number" class="hours-input" id="hoursInput" placeholder="请输入" min="0">
                <div class="hours-actions" style="display:flex; gap:10px; margin-top:16px;">
                    <button class="hours-cancel" style="flex:1; padding:12px; background:var(--bg); border:1px solid var(--border); border-radius:8px;" onclick="closeSheet('hoursSheet')">取消</button>
                    <button class="hours-confirm" style="flex:1; padding:12px; background:var(--green); color:white; border:none; border-radius:8px;" onclick="saveHours()">确认</button>
                </div>
            </div>
        </div>
    </div>

    <!-- 关联项选择弹框（通过输入框焦点或点击按钮触发） -->
    <div class="sheet-overlay" id="assocSheetOverlay" onclick="closeSheet('assocSheet')"></div>
    <div class="bottom-sheet" id="assocSheet">
        <div class="sheet-handle"></div>
        <div class="sheet-header"><span class="sheet-title">关联工作项</span><button class="sheet-cancel-btn" onclick="closeSheet('assocSheet')">关闭</button></div>
        <div class="sheet-body">
            <div class="sheet-option" onclick="addAssocItem('DEMO-24','用户权限管理优化')"><div><div style="font-size:11px; color:var(--green);">需求 · DEMO-24</div><div>用户权限管理优化</div></div><span class="check-icon">✓</span></div>
            <div class="sheet-option" onclick="addAssocItem('DEMO-23','API网关性能优化')"><div><div style="font-size:11px; color:var(--green);">需求 · DEMO-23</div><div>API网关性能优化</div></div><span class="check-icon">✓</span></div>
        </div>
    </div>

    <!-- 子项列表弹框 -->
    <div class="sheet-overlay" id="subitemSheetOverlay" onclick="closeSheet('subitemSheet')"></div>
    <div class="bottom-sheet subitem-sheet" id="subitemSheet">
        <div class="sheet-handle"></div>
        <div class="sheet-header"><span class="sheet-title">子项需求 (2)</span><button class="sheet-cancel-btn" onclick="closeSheet('subitemSheet')">关闭</button></div>
        <div class="sheet-body">
            <div class="subitem-item"><div class="subitem-title">如何选择合适的项目模板？</div><div class="subitem-meta">DEMO-25 · 待处理</div></div>
            <div class="subitem-item"><div class="subitem-title">通过组创建体验的模型设计程</div><div class="subitem-meta">MODEL-345 · 开发中</div></div>
        </div>
    </div>

    <!-- 移除父项确认 -->
    <div class="sheet-overlay" id="confirmRemoveOverlay" onclick="closeSheet('confirmRemove')"></div>
    <div class="bottom-sheet" id="confirmRemove">
        <div class="sheet-handle"></div>
        <div class="sheet-header"><span class="sheet-title">确认移除父项</span></div>
        <div class="sheet-body">
            <div style="padding:20px; font-size:14px; color:var(--text-secondary);">确定要移除已关联的父项吗？</div>
            <div style="display:flex; gap:10px; padding:0 20px 24px;">
                <button style="flex:1; padding:12px; background:var(--bg); border:1px solid var(--border); border-radius:8px;" onclick="closeSheet('confirmRemove')">取消</button>
                <button style="flex:1; padding:12px; background:var(--red); color:white; border:none; border-radius:8px;" onclick="doRemoveParent()">确认移除</button>
            </div>
        </div>
    </div>

    <!-- Toast -->
    <div class="toast" id="toast"></div>

    <script>
        // ----- 全局状态 -----
        let selectedParent = null;          // 已选父项 {id, title, hasSub}
        let continueCreate = false;
        let parentMenuVisible = false;
        let activeTab = 'tab-basic';

        // ----- 抽屉 -----
        function openDrawer() {
            document.getElementById('drawerOverlay').classList.add('active');
            document.getElementById('mainDrawer').classList.add('active');
            document.body.style.overflow = 'hidden';
            // 重置父项状态(演示)
        }
        function closeDrawer() {
            document.getElementById('drawerOverlay').classList.remove('active');
            document.getElementById('mainDrawer').classList.remove('active');
            document.body.style.overflow = '';
        }

        // ----- 标签切换 -----
        function switchTab(btn, tabId) {
            btn.closest('.segment-bar').querySelectorAll('.segment-btn').forEach(b => b.classList.remove('active'));
            btn.classList.add('active');
            ['tab-basic','tab-related','tab-fields'].forEach(id => {
                document.getElementById(id).style.display = id === tabId ? 'block' : 'none';
            });
            activeTab = tabId;
        }

        // ----- 标题字数 -----
        function updateCharCount(el) { document.getElementById('charCount').textContent = el.value.length; }

        // ----- 编辑器 -----
        function execCmd(cmd) { document.getElementById('descEditor').focus(); document.execCommand(cmd, false, null); }
        function handleEditorInput(el) { if (el.innerHTML === '<br>' || el.innerHTML === '') el.innerHTML = ''; }
        function insertLink() { let url = prompt('链接URL:','https://'); if(url) document.execCommand('createLink', false, url); }
        function insertImage() { let url = prompt('图片URL:','https://'); if(url) document.execCommand('insertImage', false, url); }

        // ----- 父项逻辑 -----
        function showParentInput() {
            if (selectedParent) {
                // 已有关联父项 -> 显示更多菜单
                toggleParentMenu();
                return;
            }
            document.getElementById('parentChip').style.display = 'none';
            document.getElementById('parentInputWrapper').classList.add('active');
            document.getElementById('parentSearchField').focus();
        }
        function hideParentInput() {
            document.getElementById('parentInputWrapper').classList.remove('active');
            document.getElementById('parentChip').style.display = 'inline-flex';
            document.getElementById('parentSearchField').value = '';
        }
        function clearParentInput() {
            document.getElementById('parentSearchField').value = '';
            document.getElementById('parentSearchField').focus();
        }
        // 父项输入框获得焦点 -> 打开选择弹框 (模拟)
        document.getElementById('parentSearchField')?.addEventListener('focus', function() {
            openSheet('parentSearchSheet');
        });
        function selectParentFromModal(id, title, hasSub) {
            selectedParent = { id, title, hasSub };
            // 更新UI
            document.getElementById('parentChip').style.display = 'inline-flex';
            document.getElementById('parentChip').classList.add('has-parent');
            document.getElementById('parentChipText').innerHTML = `${id} · ${title.substring(0,10)}…`;
            document.getElementById('parentInputWrapper').classList.remove('active');
            // 显示已选区域
            document.getElementById('parentSelected').classList.add('active');
            document.getElementById('selectedParentId').textContent = id;
            document.getElementById('selectedParentTitle').textContent = title;
            document.getElementById('parentSubCount').style.display = hasSub ? 'inline-block' : 'none';
            if (hasSub) document.getElementById('parentSubCount').textContent = '2';
            closeSheet('parentSearchSheet');
            showToast(`已关联父项 ${id}`);
        }
        function toggleParentMenu() {
            let menu = document.getElementById('parentMoreMenu');
            menu.style.display = menu.style.display === 'block' ? 'none' : 'block';
        }
        function changeParent() {
            document.getElementById('parentMoreMenu').style.display = 'none';
            document.getElementById('parentSelected').classList.remove('active');
            showParentInput();
        }
        function showRemoveConfirm() {
            document.getElementById('parentMoreMenu').style.display = 'none';
            openSheet('confirmRemove');
        }
        function doRemoveParent() {
            selectedParent = null;
            document.getElementById('parentSelected').classList.remove('active');
            document.getElementById('parentChip').classList.remove('has-parent');
            document.getElementById('parentChipText').innerHTML = '插入父项';
            closeSheet('confirmRemove');
            showToast('父项已移除');
        }
        function openSubitemSheet() { openSheet('subitemSheet'); }

        // ----- 关联项 -----
        function filterAssoc(btn, type) {
            btn.closest('.assoc-tabs-scroll').querySelectorAll('.assoc-tab').forEach(b => b.classList.remove('active'));
            btn.classList.add('active');
            // 表格行筛选
            let rows = document.querySelectorAll('#assocTableBody tr');
            rows.forEach(row => {
                if (type === 'all' || row.dataset.type === type) row.style.display = '';
                else row.style.display = 'none';
            });
        }
        function openAssocSheet() { openSheet('assocSheet'); }
        function clearAssocSearch() { document.getElementById('assocSearchInput').value = ''; }
        function addAssocItem(id, title) {
            showToast(`已关联 ${id}`);
            // 演示：不实际添加，仅关闭弹框
            closeSheet('assocSheet');
        }

        // ----- 底部弹框通用 -----
        function openSheet(id) {
            if (document.getElementById(id + 'Overlay')) {
                document.getElementById(id + 'Overlay').classList.add('active');
                document.getElementById(id).classList.add('active');
            }
        }
        function closeSheet(id) {
            if (document.getElementById(id + 'Overlay')) {
                document.getElementById(id + 'Overlay').classList.remove('active');
                document.getElementById(id).classList.remove('active');
            }
        }
        // 单选
        function selectOption(sheetId, valId, el, value) {
            el.closest('.sheet-body').querySelectorAll('.sheet-option').forEach(o => o.classList.remove('selected'));
            el.classList.add('selected');
            document.getElementById(valId).innerHTML = value;
            document.getElementById(valId).classList.remove('placeholder');
            closeSheet(sheetId);
        }
        function selectAssignee(el, email, initials) {
            el.closest('.sheet-body').querySelectorAll('.sheet-option').forEach(o => o.classList.remove('selected'));
            el.classList.add('selected');
            document.getElementById('assigneeVal').innerHTML = `<span class="avatar-sm">${initials}</span>${email.split('@')[0]}`;
            closeSheet('assigneeSheet');
        }
        function selectPriority(el, level, label) {
            el.closest('.sheet-body').querySelectorAll('.sheet-option').forEach(o => o.classList.remove('selected'));
            el.classList.add('selected');
            document.getElementById('priorityVal').innerHTML = `<span class="priority-pill priority-${level}"><span class="priority-dot ph-${level}"></span>${label}</span>`;
            closeSheet('prioritySheet');
        }
        // 多选
        function toggleMulti(el) { el.classList.toggle('selected'); }
        function saveMultiSelect(sheetId, valId) {
            let selected = document.querySelectorAll(`#${sheetId} .sheet-option.selected`);
            let valEl = document.getElementById(valId);
            if (selected.length === 0) { valEl.textContent = '请选择'; valEl.classList.add('placeholder'); }
            else if (selected.length === 1) { valEl.textContent = selected[0].textContent.replace('✓','').trim(); valEl.classList.remove('placeholder'); }
            else { valEl.textContent = `已选 ${selected.length} 项`; valEl.classList.remove('placeholder'); }
            closeSheet(sheetId);
        }
        // 日期
        function openDatePicker(field) {
            let input = document.createElement('input');
            input.type = 'date'; input.style.position = 'fixed'; input.style.opacity = '0';
            document.body.appendChild(input);
            input.addEventListener('change', function() {
                document.getElementById(field + 'Val').textContent = this.value;
                document.getElementById(field + 'Val').classList.remove('placeholder');
                document.body.removeChild(input);
            });
            input.addEventListener('blur', () => { setTimeout(() => { if(document.body.contains(input)) document.body.removeChild(input); },200); });
            input.click();
        }
        // 工时
        function openHoursSheet() { openSheet('hoursSheet'); setTimeout(()=>document.getElementById('hoursInput')?.focus(),300); }
        function saveHours() {
            let val = document.getElementById('hoursInput').value;
            let text = (val && val !== '0') ? `${val} 小时` : '预估';
            document.getElementById('hoursText').textContent = text;
            closeSheet('hoursSheet');
        }
        // 继续新建
        function toggleContinue() {
            continueCreate = !continueCreate;
            document.getElementById('continueToggle').classList.toggle('on', continueCreate);
        }
        // 新建需求
        function createDemand() {
            if (!document.getElementById('demandTitle').value.trim()) {
                showToast('请填写需求标题'); return;
            }
            showToast('需求创建成功 ✓');
            if (!continueCreate) { setTimeout(closeDrawer, 800); }
        }
        // Toast
        function showToast(msg) {
            let t = document.getElementById('toast');
            t.textContent = msg; t.classList.add('show');
            clearTimeout(t._timer); t._timer = setTimeout(()=>t.classList.remove('show'), 2000);
        }

        // 初始化
        updateCharCount(document.getElementById('demandTitle'));
        // 给父项输入框添加焦点弹框
        window.onload = function() {
            if (document.getElementById('parentSearchField')) {
                document.getElementById('parentSearchField').addEventListener('focus', function() { openSheet('parentSearchSheet'); });
            }
        }
    </script>
</body>
</html>
```